{
  "name": "__APP_SLUG__",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "pnpm prepare:design-system && tsc --noEmit && vite build",
    "check": "pnpm prepare:design-system && pnpm lint && pnpm test && pnpm build",
    "dev": "vite",
    "dns": "node scripts/configure-domain.mjs",
    "lint": "tsc --noEmit",
    "prepare:design-system": "node scripts/prepare-design-system.mjs",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.7",
    "firebase": "12.16.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.0.4",
    "typescript": "^6.0.0",
    "vite": "^7.1.12",
    "vitest": "^4.0.0"
  }
}

