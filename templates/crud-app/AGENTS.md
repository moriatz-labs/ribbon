# __APP_TITLE__ Agent Contract

This project was generated by VSCD. Use the global `vscd-build` skill for scaffolding, provider linking, release work, and URL handoff.

## Non-negotiable design-system contract

- Read `C:\Users\loqpm\Documents\UI\AGENTS.md` and `C:\Users\loqpm\Desktop\Paul\design-system\AGENTS.md` before any frontend change.
- Paul's private design system is the component floor, not an optional visual reference. Import public APIs from `@paul/ui-core`, `@paul/ui-icons`, and `@paul/ui-patterns`; import `@paul/ui-tokens/styles.css` at the application root.
- Use `DatePicker` from `@paul/ui-patterns` for every date field. Native `input[type=date]`, third-party calendar packages, Tailwind, shadcn/ui, Lucide, and direct Radix imports are forbidden in generated apps.
- Before creating a local component, search the design-system catalog. Local code may compose page-specific layout, but it must use semantic design tokens and must not recreate an available primitive, pattern, icon, state, or interaction.
- Keep `DESIGN_SYSTEM_COMMIT` pinned in `vscd.json`, `.env.example`, and CI. Remote builds must receive the server-only `DESIGN_SYSTEM_DEPLOY_KEY` secret and run `prepare:design-system`.
- Keep `.vercel-design-system/**` in `vitest.config.ts` alongside Vitest's default exclusions. Product tests must never collect the private design-system repository's own test suites.
- `vscd check` is the enforcement gate. Do not suppress, bypass, or delete its design-system checks.
- Keep `projectType: "application"` in `vscd.json`. The `control-plane` type is reserved for the VSCD repository and is not an application escape hatch.

Before release:

1. Run `pnpm check` with the pinned private design system available.
2. Run the VSCD Codex check from the VSCD repository: `pnpm vscd check <this-project-path>`.
3. Run `supabase test db` against the local stack.
4. Provision Vercel CNAME records through Hostinger DNS using the VSCD control plane.
5. Deliver magic links and transactional mail through the server-only Hostinger Mail adapter generated under `api/`; keep the rate-limit RPC and secret environment contract intact.
6. Release through a pull request and GitHub Actions.

Never expose service-role, Supabase management, Vercel, Hostinger DNS/mail, or Cloudflare tokens in browser code. Do not replace Hostinger Mail with a browser SDK or a second mail provider unless the user explicitly requests it.

