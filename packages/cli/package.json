{
  "name": "@moriatz/ribbon-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ribbon": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@moriatz/ribbon-core": "workspace:*"
  }
}

