{
  "name": "@vscd/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "vscd": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vscd/core": "workspace:*"
  }
}

